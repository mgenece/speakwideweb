import { AuthContent } from '@/styles/StyledComponents/AuthWrapperStyled';
import SignupInterpreter from '../layouts/authentication/interpreter/SignupInterpreter';

export default function SignupInterpreterTab() {
  return (
    <AuthContent>
      <SignupInterpreter />
    </AuthContent>
  );
}
